# Binary image section area analysis input file
# Run with:
#   python examples/image_processing/analyze.py examples/image_processing/input.txt
#
# Notes:
# - image_path is required.
# - output_report can be left blank; then the code creates: <image_name>_analysis.txt
# - threshold must be between 0 and 255.
#   Pixels with intensity greater than the threshold become white, and the rest become black.
# - connectivity = 1 means 4-connectivity (edge-sharing pixels only).
#   connectivity = 2 means 8-connectivity (diagonal neighbors also connected).
# - invert = true swaps black and white after thresholding.
# - include_white = true analyzes white sections.
# - include_black = true analyzes black sections.
# - At least one of include_white or include_black must be true.

[INPUT]
image_path = test.jpg
output_report = myimage_analysis.txt
analyzed_image = analyzed.png
threshold = 127
connectivity = 2
invert = false
include_white = true
include_black = true
save_analyzed_image = true
replica_count = 5
random_seed = 12345
min_black_region_size = 500
max_black_region_size = 5000
boundary_smoothing_sigma = 1.2
boundary_cleanup_width = 2
